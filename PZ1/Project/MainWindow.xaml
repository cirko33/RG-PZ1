<Window x:Class="Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project"
        mc:Ignorable="d"
        Title="RACUNARSKA GRAFIKA PZ1" Height="800" Width="1000">
    <DockPanel>
        <Slider x:Name="Zoom" Minimum="0.1" Maximum="10" Value="1" DockPanel.Dock="Left">
            <Slider.LayoutTransform>
                <RotateTransform Angle="270"></RotateTransform>
            </Slider.LayoutTransform>
        </Slider>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="MainCanvas" DockPanel.Dock="Bottom" Height="1000" Width="1000" Focusable="True">
                <Canvas.LayoutTransform>
                    <ScaleTransform x:Name="ScaleTransform" 
                                    ScaleX="{Binding ElementName=Zoom, Path=Value}"
                                    ScaleY="{Binding ElementName=Zoom, Path=Value}"/>
                </Canvas.LayoutTransform>
            </Canvas>
        </ScrollViewer>
    </DockPanel>
</Window>
